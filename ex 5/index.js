const nome = 'Luis'; //string
const nome1 = "Luis"; //string
const nome2 = `Luis`; //string
const num1 = 10; //number
const num2 = 10.52; //number
let nomeAluno; //undefined -> não aponta para local algum na memória, valor dado automaticamente
const sobrenomeAluno = null //nulo -> não aponta para local algum na memória, valor declarado
const aprovado = false; //boolean(booleano) = tru ,false ( lógico)

console.log(typeof aprovado, aprovado)
